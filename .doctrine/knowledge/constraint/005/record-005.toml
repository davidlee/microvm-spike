schema = "doctrine.knowledge"
version = 1

id = 5
slug = "only-the-guard-can-read-the-inside-of-a-capsules-namespace"
title = "Only the guard can read the inside of a capsule's namespace"
record_kind = "constraint"
status = "active"
created = "2026-08-16"
updated = "2026-08-16"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
statement = "The perimeter guard is the sole reader of the inside of a capsule namespace, so nothing can independently confirm ip_forward=0; acceptable only because the guard holds egress bound to itself."
source = "technical"
applies_to = ["capsule-perimeter-guard", "host/netns.nix"]
waiver_reason = ""
waived_by     = ""
waived_on     = ""

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)
