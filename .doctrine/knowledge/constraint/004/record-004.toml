schema = "doctrine.knowledge"
version = 1

id = 4
slug = "a-rotated-secret-does-not-reach-a-running-capsule"
title = "A rotated secret does not reach a running capsule"
record_kind = "constraint"
status = "active"
created = "2026-08-16"
updated = "2026-08-16"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
statement = "Every injected payload is write-if-absent, so a rotated secret does not reach a running capsule; refreshing at start would silently discard guest writes."
source = "technical"
applies_to = ["capsule-inject"]
waiver_reason = ""
waived_by     = ""
waived_on     = ""

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)
