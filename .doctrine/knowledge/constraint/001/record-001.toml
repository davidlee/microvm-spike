schema = "doctrine.knowledge"
version = 1

id = 1
slug = "probe"
title = "Extras are fleet-wide, so nothing pins them"
record_kind = "constraint"
status = "active"
created = "2026-08-16"
updated = "2026-08-16"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
statement = "One extras list for the fleet, so one guest image; the assignment record's extras and image fields are inert and nothing pins a composition."
source = "technical"
applies_to = ["fragments.nix", "flake.nix", "assignment record"]
waiver_reason = ""
waived_by     = ""
waived_on     = ""

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)
