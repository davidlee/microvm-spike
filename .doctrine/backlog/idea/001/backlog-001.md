# IDE-001: Drive a doctrine slice from a skill

**Parked deliberately, not forgotten.** `capsule-run`-shaped, with argv supplied
by doctrine so **this repo never learns what a slice is**.
`docs/contract-doctrine.md`'s Role 3 says where it would attach.

SL-254 was driven by hand and SL-251 is being driven by hand — those are
*doctrine's* slice ids, in the confined corpus, not this repo's (`ADR-001` term
2). **The sideband arc is what that hand-driving turned out to need**: collect
the half that is not a commit, adopt it, brief the next capsule with it, scope
what a collect takes.

`NOTES item 42` and `NOTES item 45` are the same lesson from the other end — the
*first* capsule on a unit needs the same half, nothing had a way to give it one,
and when the case arrived the capsule had already started.

**What to do meanwhile:** the next hand-driven slice repeats the requirements
list Role 3 is waiting on. Write it down as it happens rather than reconstructing
it afterwards.

Evidence rung (`STD-001`): unbuilt, and deliberately so until the requirements
list stops growing.
