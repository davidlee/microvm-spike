# CHR-002: Exercise item 53's three verbs on a live slot

`NOTES item 53`'s verbs — `setup` writing `unit`/`purpose`, plus `handoff` and
`land` — have run **in a sandbox and nowhere else**. The verify, the archive, the
drop and the force have never touched a live slot.

Both `handoff` and `land` collect first and then refuse unless the guest's head
is one of the objectnames the quarantine's code refs hold. There is **no
`--stale`**, whose corollary is that both need the source *running*.

The cheap shape: a free slot handed a finished exhibit, **watched rather than
assumed**. `f`…`j` are declared and uncreated, and `c`'s work has already
migrated to `d`, so neither destination costs a live assignment.

Needs CHR-001 first, or it exercises a host that does not have the code.

Evidence rung (`STD-001`): currently **run** (sandbox). This buys **take** and,
for the two refusals, **trigger**.
