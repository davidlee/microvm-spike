# The ledger — one numbered item per question

**Cited from source and from the other docs as `NOTES item N`.** That citation
is an *id*, not a path: the numbers are frozen and append-only, a resolved item
is struck or annotated in place, never deleted and never renumbered, and the
file it lives in may change without any of the source comments citing it
changing with it.

**One file per item, and the number is the filename** — `NNN-slug.md`, so
`NOTES item 20` is `020-*.md` and reading one item costs one item rather than
1400 lines. The slug is a convenience and the number is the identity; if a slug
ever misleads, rename the file and leave the number alone.

**Adding one:** a new file with the next number, a row at the bottom of the
table, and a state. Nothing else moves.

Resolved items are kept because the reasoning is the value — several of them
record a wrong first answer next to the measurement that corrected it, which is
what stops it being proposed again. Present tense belongs to
[status.md](../status.md) and figures to [probes.md](../probes.md); an item that
has become a bare status flag should shrink to a pointer at one of them the next
time it is touched.

| # | item | state |
| --- | --- | --- |
| [1](./001-what-has-been-run.md) | what has actually been run, versus reviewed | standing caveat |
| [2](./002-agent-credentials.md) | agent credentials into a guest with no shares | open |
| [3](./003-claude-code-unfree.md) | `pkgs.claude-code` is unfree, and guarded for channel drift | resolved |
| [4](./004-live-postgres.md) | `just test` may want a live Postgres | open, unhit |
| [5](./005-doctrine-binary-in-guest.md) | no `doctrine` binary in the guest | resolved — `dev-tools` |
| [6](./006-proxy-env-login-shell-scope.md) | proxy env is login-shell scope, so units don't inherit it | accepted |
| [7](./007-host-config.md) | host config — the accept, the forward drop, and checking the drop | resolved, plus one host edit outstanding (item 18) |
| [8](./008-git-daemon-unauthenticated.md) | git-daemon is unauthenticated | resolved by deletion (item 18) |
| [9](./009-egress-allowlist-unproven.md) | egress allowlist unproven; and the `MaxClients` hang that was not it | half — slots fixed, list still unproven |
| [10](./010-vendored-crates-dropped.md) | vendored crates and pre-seeded `node_modules`, dropped | decision |
| [11](./011-host-side-runs-as-you.md) | everything host-side runs as you | services half done, VMM half open |
| [12](./012-no-resource-ceiling.md) | no resource ceiling on the VM | open |
| [13](./013-host-smt-on.md) | host SMT is on | accepted, not fixed |
| [14](./014-hypervisor-choice.md) | hypervisor choice — firecracker's floor is what shapes half this list | open option |
| [15](./015-things-that-only-grow.md) | two things that only grow: the volume, and the proxy log | measured, accepted |
| [16](./016-target-agnostic.md) | target-agnostic | done, and a second target has since exercised it — item 23 |
| [17](./017-more-than-one-capsule.md) | more than one capsule at a time | scoped — [Plan C](../plan-c-multi-capsule.md) |
| [18](./018-git-channel-direction.md) | which way the git channel points | measured, inverted, done |
| [19](./019-baseline-build-and-figures.md) | the baseline build, and where a figure is allowed to live | built, run, measured |
| [20](./020-which-capsule-a-program-means.md) | which capsule a host program is talking to | decided, built, run at N=2 on both paths |
| [21](./021-declared-capsule-flake-attribute.md) | a declared capsule needs a flake attribute, and all of them are one value | built and run at N=2 |
| [22](./022-secrets-at-start.md) | secrets at start, and a payload that may be absent | built and run at N=2 |
| [23](./023-second-target.md) | a second target, and what the parameterisation actually cost | done on branch `second-target` |
| [24](./024-set-u-not-login-shell.md) | a `set -u` script must not *be* the login shell | fixed and shipped, unwitnessed |
| [25](./025-assignment-is-a-perimeter-verb.md) | assignment is a perimeter-mutating verb | scoped in [contract-assignment.md](../contract-assignment.md); **being resolved by item 36**, half built |
| [26](./026-project-nix-runs-on-the-host.md) | a project's flake is code that runs on the host | open — scoped in [contract-flavour.md](../contract-flavour.md) |
| [27](./027-a-class-is-not-always-a-kilobyte.md) | a class is a kilobyte only over reservations the profile derives nothing from | settled for `mem` by eval, `vcpu` read from source |
| [28](./028-a-slot-has-no-default.md) | a slot has no default, and a front end is where one is guessed | built and run at N=2 |
| [29](./029-the-record-is-front-end-written.md) | the record is front-end written, and a pin is measured rather than resolved twice | built and run on this host |
| [30](./030-a-pool-audits-what-exists.md) | a pool degrades by auditing what exists, not by excluding what is declared | reviewed, built, run — closes [Plan D](../plan-d-fleet.md) L12, and **the pool is declared and switched**: `a`…`j`, costing 3% of an eval and one page of PID 1 at rest, guard reading `2 of 10` |
| [31](./031-the-fragment-vocabulary.md) | the fragment vocabulary: composition built, selection deferred | built, run, measured — supersedes item 3; selection is [Plan D](../plan-d-fleet.md) D7 |
| [32](./032-the-sideband-channel.md) | the sideband channel: state that is not a commit | built and run; extraction is item 34 and the inbound half is item 35, both now run — **the scope is closed**: `statePaths` is a template, the assignment fills `{unit}`, and the same exhibit is 36 entries against 1886 |
| [33](./033-provision-is-a-sequence.md) | a provision is not finished when the push lands | built, evaluated and **run** — inside a provision and standalone; step (3) of item 32's inbound half |
| [34](./034-adopting-a-guest-authored-tree.md) | adopting a guest-authored tree: what actually needed checking | built, evaluated, logic asserted against real git objects, **run** on the real 1886-entry exhibit |
| [35](./035-briefing-a-capsule-with-state.md) | briefing a capsule with another one's state | built, evaluated, guest half asserted at build time (`briefCases`), **run** across two live capsules — closes item 32's inbound step (2) |
| [36](./036-a-policy-is-selected-not-named.md) | a policy is selected from a declared set, never named by a project | **built, switched and proven at the wire** — the vocabulary, each slot's set, the allowlist and both collect limbs, plus `capsule <slot> policy <name>` and 28 cases; `probe-netns-egress` 33/33 with stage 2b green, so a selected allowlist is what a guest meets. **and the last claim is made**: `probe-two-capsules` run 3 puts `build` and `sealed` on two guests at the same moment, `200` for one and `403` for the other, swapping when the policies do — fourteen assertions green ([probes](../probes.md#run-3--two-capsules-two-policies-at-the-same-moment)). Only `sealed` on a *declared slot* is left | — **and finished at the wire by the units**: `mayCollect` refused `capsule b collect` under `sealed`, the verb's proxy-restart branch ran six times, and `a` on `build` answered `200` while `b` on `sealed` answered `403 Filtered` through their own `capsule-proxy-<slot>` units ([probes](../probes.md#what-the-units-own-perimeter-answered-the-first-time-one-ran)). **Closes [Plan D](../plan-d-fleet.md) L3 and S12** — a policy is selected per slot, so two projects with different dependency hosts stop sharing a union allowlist, and by the shape §6 predicted rather than the per-capsule *field* L3 itself proposed
| [37](./037-a-teardown-that-only-unnames.md) | a teardown that only unnames, and the check that could not have caught it | **fixed, switched and instrumented** — both netns programs roll back an aborted `up`; `hostModulePrograms` builds every program the module's units name, which nothing did before; and `probe/netns-restart.sh` runs the real `capsule-netns` on nobody's addressing, 33/33 against 30/3 pre-fix ([probes](../probes.md#what-netns-restartsh-established)). The probe **withdrew this item's first reading of its own evidence**: the race is the least instrumentable part of the bug and the smallest part of it |
| [38](./038-a-probe-that-became-a-borrower.md) | a probe that became a borrower without changing | **fixed and enforced, unrun** — `probe/netns-egress.sh` shared its namespace, both links, both addresses and its /16 with the live aggregator — and `probe/netns.sh` the addressing, in the **root** namespace — because `capsules.nix` was written from those probes' map afterwards; its teardown deletes `eg-rt` by name. The fabric is `probe/harness.sh`'s now, on `flake.nix`'s `probeFabric`, which **throws at eval** on any string `capsules.nix` declares — six mutations, six refusals. **Both probes have since run green on the new fabric**, and the run turned up a third instance one level down — an assertion written against a ref convention that moved |
| [39](./039-a-bind-is-not-a-traversal.md) | a bind is mounted as root and opened as the user | **fixed and asserted at eval, switch owed** — every `capsule-proxy-<slot>` was unable to start: `BindReadOnlyPaths` named its allowlist under `stateDir` (`0750 owner:users`) and the proxy runs as `capsule-proxy`, so the mount succeeded as root and the open failed as the unit. The link is in its own `allowlistDir` now, which is [item 36](./036-a-policy-is-selected-not-named.md)'s separation made true of the filesystem; `hostModuleUnits` **throws** when a unit binds a path under a module-declared directory its user cannot traverse, watched red on the pre-fix path. It survived a switch, 28 cases and two wire proofs because **a control can be switched and proven and still never have been started** | — **now proven by a running slot**: both proxies came up `running` and each served a real request, which is the evidence this item said nothing in this repo could produce
| [40](./040-no-doors-is-not-the-other-shape.md) | a fallback selected by absence, and the ten seconds a status row spent proving it wrong | **fixed and measured** — `host/cli.nix`'s `door` inferred which transport reaches a capsule from whether any *other* slot's relay socket was open, so a module-path host at rest read as the devshell path and every `capsule all status` row ssh'd straight at `net.guest` for a full `ConnectTimeout`: 10.06 s a row, **0.375 s for ten** once it asks the precondition the direct transport actually has (a tap in this namespace). The refusal that was supposed to fire instead — "the module path owns this host" — had **never fired once**, because it needed some other capsule to be up. Not case-covered: both arms are host state ([probes](../probes.md#what-a-fleet-status-costs)) |
| [41](./041-a-delegable-verb-that-ends-in-root.md) | a delegable verb that ends in root, and the half-write when it cannot | **fixed and asserted, switch owed** — `capsule <slot> policy <name>` ended in `sudo systemctl restart capsule-proxy-<slot>` with no rule permitting it; the branch had never run, and ran once on a sudo ticket a `just up` left warm. The record and link move under one lock and the wire was a third thing outside it, so a failed restart was **fail-open in the one direction that matters**. The restart is inside the hook now — a failed bounce puts the link back and writes no document, so the refusal that says *none of them moved* is true — and the module grants exactly that one restart per declared slot, `NOPASSWD`, no wildcard, at sudo's resolved path and not a store path. `policyCases` 32 → 44 via a `proxyControl` argument (a case cannot stub `systemctl`, which `runtimeInputs` puts ahead of PATH), red on four assertions under a plausible mutation; `hostModuleUnits` throws when a proxy unit has no rule. **A rollback beats a check: authority is not authentication, and a ticket can expire between the two** | — **done: the verb reaches root unattended**, `sudo -K` then `capsule b policy sealed` with no prompt (generation 21) and back to `build` (22). The rollback fired for real twice on the way, each time correctly, and each firing was a fault this repo could not see — the grant shadowed ([43](./043-a-grant-that-was-present-and-inert.md)), then the rule naming a command the program did not run ([44](./044-a-rule-matches-a-path-not-a-name.md))
| [42](./042-a-state-half-no-capsule-has-held.md) | a state half no capsule has ever held | **delivered** — on slot `e`, as step (2) of one provision (`capsule e provision <sha> --state-from-host`, 5.204 s end to end and the state half ≈0.65 s of it), with the host-side `code-oid` precheck fired first and deliberately, writing nothing. Getting there cost [item 47](./047-a-script-on-stdin-and-the-command-that-eats-it.md), which is why the verb is a flag on a provision rather than the second of two commands this item expected. Before that, **decided and built; unrun against a guest** — a capsule taking on a *fresh* unit needs out-of-band state that is in the human's checkout and has never been inside anything, and every inbound motion read from a quarantine. `capsule-brief --from-host` is the origin that is not a capsule: `snapshotFor`'s own text at `target.path`, delivered over the door a brief already had. Two decisions, both in this commit — **a quarantine is what a capsule sent back**, so this keeps no archive at all (the ref is dropped either side, the objects are gc's) and `briefCheckSpec` refuses any source that is not a declared slot; and the untracked sweep is an **argument** with no default, because "one agent's uncommitted work" is a property of a capsule and is a desk on a host — 30 entries against 34, three of the four extras written by another process seconds earlier. `code-oid` stays a reading of HEAD, so the price is sequencing: provision at the checkout's commit, then brief. The item's own suggested mechanism (`GIT_DIR` at a quarantine) is refuted in one command |
| [43](./043-a-grant-that-was-present-and-inert.md) | a grant that was present and inert | **fixed, asserted and switched — and it was not the only thing wrong** ([44](./044-a-rule-matches-a-path-not-a-name.md)) — [item 41](./041-a-delegable-verb-that-ends-in-root.md)'s `NOPASSWD` rule was in the rendered sudoers, matched the command and was listed back by `sudo -n -l`, and a `%wheel ALL=(ALL:ALL) ALL` one line below it took every match: sudoers is last-match-wins, and a plain definition lands at priority 1000 where *module merge order* decides. The only reason anyone looked is that **item 41's rollback fired for real** — cold ticket, `capsule-proxy-b would not restart, so the selection was undone`, link back and no document, which is that item's claim proven live. The rule is `lib.mkAfter` now and a third assertion reads the **rendered** `security.sudo.configFile` for any later untagged line covering the owner, vacuous in the standalone eval and firing at the switch; watched red at `mkOrder 1600` and green at 1000. `~/flakes`' copy of the blanket is redundant against nixpkgs' own at `mkOrder 600` and is the user's to drop. **`sudo -l` answers about the ruleset; only a call answers about the call** |
| [44](./044-a-rule-matches-a-path-not-a-name.md) | a rule matches a path, not a name | **fixed, switched and exercised — the verb reaches root unattended**, `sealed` and back on a cold ticket, generations 21 and 22, by the module's own copy; which also takes item 36's last owed exercise, the in-place restoration — with [43](./043-a-grant-that-was-present-and-inert.md)'s shadow gone and the grant genuinely last, `capsule b policy sealed` still asked for a password. This host sets no `Defaults secure_path`, so sudo resolves an unqualified command against the **caller's** `PATH` — and `writeShellApplication` prepends `runtimeInputs`, so the front end's `systemctl` is a store path and the `/run/...` rule names a different command. [Item 41](./041-a-delegable-verb-that-ends-in-root.md) wrote that reason down backwards; its *choice* stands, since a store-path rule would lapse at the next systemd bump, fail-open and silent. The command is one value now (`host/proxy-restart.nix`) imported by the program, the rule and `unrestartable` — which was green because it **paired the module against itself**, both sides of the question coming from one file and the program never in it. Third item running where `sudo -n -l` printed the command back and meant nothing: it reports what would run, not whether it would run free |
| [45](./045-a-brief-is-an-origin-not-a-top-up.md) | a brief is an origin, not a top-up | **half answered** — the composite this item argued for is built and has run (`capsule-provision --state-from-host`, [item 47](./047-a-script-on-stdin-and-the-command-that-eats-it.md)), and item 47 supplied a second and sharper reason for it: on a target whose refresh writes tracked files there is no moment *after* a provision when a brief can land at all. **Still open: whether a top-up is a scoped additive verb or a refusal**, which the delivery does not settle either way, since it removes the window rather than serving a capsule already inside it. Originally **open, and hit before the verb it belongs to had ever run** — slot `c` was two hours into `SL-251` when the state half [item 42](./042-a-state-half-no-capsule-has-held.md) exists to deliver turned out to be missing from it, and `capsule-brief --from-host` could not deliver it for four reasons that compound and are each correct: the host checkout was two commits ahead so `code-oid` refuses; the guest had one modified tracked file under a declared path so the brief refuses; had it not refused it would have overwritten that file; and a brief is whole-tree, so it would also have rewritten `phases/`. Committing the agent's edit clears the second by making the first unfixable, so **the window in which a capsule can be briefed is the window before its agent starts working**. Fixed by hand — `tar` over the door, scoped to the three absent paths, `-k`, seven checksums matched and nothing overwritten — which is additive and therefore defensible and carries **no `code-oid`**, the shape item 42 rejected `rsync` for. Open: whether a top-up is a scoped additive verb or a refusal that says *brief before you start*; and this is the argument for `capsule-provision --state-from-host`, which item 42 declined to build until the delivery made someone want it. **Not evidence that `--from-host` works**: nothing here pushed, laid out a tree or ran the precheck — it covers *hit*, not *deliver* |
| [46](./046-bash-until-the-record-stops-being-flat.md) | bash, and the three things that would end it | **decided, and the decision is to change nothing** — the host programs stay `writeShellApplication` shell, and what is recorded is the *trigger* rather than the verdict. The six failures that look like a language problem ([24](./024-set-u-not-login-shell.md), [44](./044-a-rule-matches-a-path-not-a-name.md), `wait -n`, `just`'s `{{...}}`, a newline in a unit directive, the wrapper's `export`) are one class and it is not expressiveness: **structure passed through text or environment**, at a boundary — systemd, sudo, `just`, a login shell — where no language is present. Every fix was the same move, *pass argv and take an argument*. What shell buys is structural: a guest-side program **pushed at each call** cannot skew with its host, and the fragment seam that `guardCases`/`briefCases`/`snapshotCases`/`policyCases` rest on is a text seam. The strain point is named — `jq`, concentrated in `host/record.nix` — and so are the three triggers: the record stops being flat (nearest is D6's `generation` refusal, a read-compare-write), a case suite cannot reach a branch, or a verb is slow because of what it *computes* rather than what it waits for. None has fired |
| [47](./047-a-script-on-stdin-and-the-command-that-eats-it.md) | a script on stdin, and the command that eats it | **fixed twice over, cased, switched, delivered — and the composite it forced has since run from its other origin**: `capsule d provision refs/capsule/c/heads/work --state c` handed slot `c` whole to `d` six hours into `SL-251`, 9.618 s, `d`'s HEAD left at the commit it was provisioned at ([probes](../probes.md#a-capsule-handed-to-another-slot--what---state-capsule-costs)). The sequence written down for that migration was `setup` then a separate `brief`, which is **this item's window** and would have refused. Before that, **the delivery it was blocking has run** ([item 42](./042-a-state-half-no-capsule-has-held.md)) — `host/guest-exec.nix`'s `loginRun` is `bash -l -c 'bash -s'`, so a host-authored guest script **is** the guest shell's stdin and a target command that reads stdin reads *the rest of the script*: bash then has nothing left to run and exits 0. doctrine's refresh is a TUI, so **everything below `( ${command} )` in `host/refresh.nix` had never executed** — the status check, the `after` snapshot, the commit, all five messages — and a failing refresh reported success, which is precisely the trap that file's own header names. Found at step 4 of [item 42](./042-a-state-half-no-capsule-has-held.md)'s sequence, by a fresh capsule's push being refused `Working directory has unstaged changes`. The suite written to pin the fix found a **second** defect: `before`/`after` were `--porcelain` *status lines*, so a refresh rewriting an already-modified file reads as no change and the dirty-tree refusal is unreachable in exactly the case it is about — `git diff HEAD` now, content and not filenames. And the ordering neither defect causes is settled: with a refresh that writes tracked files there is no *after* a provision — it either commits, moving HEAD off what `code-oid` compares against, or leaves the tree dirty, which the brief refuses — so the brief moved **inside** the provision at step (2), which is `capsule-provision --state-from-host`, the composite [item 45](./045-a-brief-is-an-origin-not-a-top-up.md) asked for and item 42 declined to build in advance. `refreshCases` is the fifth instance of the third kind of check, and its invocation is load-bearing: it runs the script on stdin the way a guest does, because `bash <script>` passes against the broken text |
| [48](./048-a-forwarded-port-is-host-state.md) | a forwarded port is host state, and nothing allocates one | **open, nothing built** — moved out of [Plan D](../plan-d-fleet.md) L10/S9, the one limitation there with no direction covering it. *Reach* already exists: the door is ssh with a `ProxyCommand`, so `-L` through the relay socket works today. What is missing is **allocation** — nothing stops two capsules claiming one host port, nothing records which owns which, and `capsule <n> ssh` takes trailing arguments as the remote command so a `-L` cannot be said through the CLI at all. Three decided rules give it its shape without anything new being decided: the guest never chooses (naming your own host port is [item 25](./025-assignment-is-a-perimeter-verb.md)'s class, and asking for a forward is [item 18](./018-git-channel-direction.md)'s inversion a fourth time), the allocation belongs in the record rather than in a program ([20](./020-which-capsule-a-program-means.md), [29](./029-the-record-is-front-end-written.md)), and it refuses without one ([28](./028-a-slot-has-no-default.md)). **Not a perimeter verb** — the guest gains no egress, no route and no listener — but `-g` or a `0.0.0.0` bind would be, so the bind address is the control |
| [49](./049-who-owns-a-state-directory.md) | who owns a state directory, and the read nobody has taken | **both reads taken, the failure they found is not the one this item was written about, and the constraint that replaces it is asserted — built and switched. D3 and D7 are ungated.** Moved out of [Plan D](../plan-d-fleet.md) §5, where it was a caveat inside a costing section and therefore tracked by nothing. The premise holds: microvm.nix's `build()` refuses when a `toplevel` symlink is present (*"managed fully declaratively and cannot be updated manually"*), so a program of ours owning `current` can make upstream's `-u` **refuse** instead of silently rebuilding `nixosConfigurations.<slot>` — the right attribute today and the **wrong** one the moment `current` is a composition somebody chose (D7). First read: `install-microvm-<name>` writes four lines — `mkdir`, `ln -sTf <runner> current`, a `chown`, and one of `toplevel`/`flake` — and `booted` belongs to `microvm-set-booted@`, so the units want `current/bin/*`, `booted/` and the right ownership and **nothing else**. Second read: **nothing reconciles.** The only code iterating `${stateDir}/*` is a read-only activation `echo`; `microvms.target` wants only declared VMs and on this host wants nothing. So the feared loss of a volume does not exist at this lock — but the **mirror image** does: `install-microvm-*` has no `ConditionPathExists` guard unless `updateFlake` is set, so it runs on **every rebuild** and `ln -sTf`s `current` back to the declaration, silently and fail-open ([41](./041-a-delegable-verb-that-ends-in-root.md)) — **and the marker does not stop it**, since `[ -e toplevel ]` is tested only in the `microvm` command. What guards it is that `~/flakes` declares no `microvm.vms`, which it already refuses to do for an unrelated reason (`target.follows`): two independent supports for one line, the second accidental. That line is now an **assertion** in `capsule-perimeter` rather than a comment two repos away — vacuous in `hostModuleUnits`' standalone eval, which does not import microvm.nix's host module, and fired in a throwaway eval carrying both. Switched, and **switched is not witnessed**: an assertion that holds renders nothing, so what the switch establishes is that the module still evaluates on a real host, not that the refusal works |
| [50](./050-a-quarantine-outlives-its-assignment.md) | a quarantine is keyed by a slot, and a slot outlives an assignment | **third finding fixed and green; the key is still the slot** — `capsule <slot> fetch` fetches the code and state namespaces separately and answers for each on its own line, so the half that landed and the half that did not are both named, and the remedy it prints is the **archive** (`refs/capsule/<slot>/gen/<n>/`, off the record's own generation) rather than the `+` that would make the first assignment unreachable in the one place it is durable; a sweep now runs every slot and fails at the end, where `set -e` on a rejected fetch cut it short wherever it had got to. Six rounds in `policyCases` off two commits from one base plus a state chain that fast-forwards over them, since reaching it live costs two assignments to one slot; three mutations each red on its own rounds, and a fourth **rejected by shellcheck before the suite ran**, which reads exactly like nothing went red. Run on this host against slot `e` both ways. The **key** is untouched, and its disposition is now [53](./053-three-coarse-verbs.md)'s decision 3: the archive lives in the human's repository, the quarantine is not one, and `handoff` cannot land until it is automatic. Before that: **run, and the reading under it was half wrong** — two assignments to slot `e`, one quarantine ([probes](../probes.md#two-assignments-one-quarantine)). The **code** half is as predicted: `+ 0ab546b6c...592168676 (forced update)`, and `fsck` afterwards reports the first assignment's commit `unreachable`. The **state** half was never forced — it *fast-forwarded*, because the guest parents each snapshot on its own `refs/capsule/state/<stage>`, which lives on the volume and which a provision does not touch, so the chain crosses the reprovision and is rooted in a commit **this host** wrote (the `--state-from-host` delivery; `+1000` against the guest's `+0000`, which makes the timezone a free authorship tell). A refspec's `+` says what a fetch *may* do and never what it did. Third finding: `capsule <slot> fetch`'s refspec is **unforced** where the collect's is forced (`host/cli.nix:798`), so the second fetch is `! [rejected] (non-fast-forward)` for code and a fast-forward for state — the human's repository ends holding **assignment 1's code beside assignment 2's state**, under two names that say they belong together, and the verb exits 1 having half-succeeded without a line of its own about either. So a `generation` in the key fixes the code half and nothing else. **Not built**; `c`'s quarantine was deliberately left unspent. Before that, **open, and the sharper half is unhit** — moved out of [Plan D](../plan-d-fleet.md) S10, where retention was one clause of a user story. [Item 42](./042-a-state-half-no-capsule-has-held.md) decided *what* a quarantine is — what a capsule sent back, not a place state lives, which is why `--from-host` keeps no archive — and that sharpens retention rather than settling it: an artifact of one past collect has a lifetime and nothing says whose it is. The half underneath, read from source and never reached by a run: `host/quarantine.nix` keys both ref halves on the **slot** (`refs/capsule/<name>/heads`, `…/state`) and `host/git-channel.nix`'s collect refspecs are **forced**, so a slot's second assignment overwrites the first's refs by name in the first's quarantine. Forcing is right *within* an assignment — a guest chooses its own ref names — so the question is whether the slot is the right key, and the record already carries the better one (`generation`, written and never read). And `fetch` lands the same namespace in the human's repo, which makes that repo the accidental archive: whether a past result still exists depends on when somebody happened to run a verb. Cheap to hit — collect, reprovision, collect, then read the ref |
| [51](./051-the-target-in-four-store-paths.md) | the four programs still spell the target, and it is item 20 one level up | **closed: steps 0-4 and 6 built and green, all four decisions taken; step 5 belongs to the commit that takes the documents out of the store** — [Plan D](../plan-d-fleet.md) §6.4, which that file calls D7's *first task* rather than a detail of it, moved here because the implementation will not fit one session and a plan is not a thing anyone hands over. `host/programs.nix` interpolates `target.nix`'s values into every host-side program, so a store path is a function of which project this host confines: one target makes that invisible, two make it four programs per target — the same shape as a socket path baked into a store path meaning a program per capsule ([20](./020-which-capsule-a-program-means.md)). The inventory is read from source, and the scope moved with it: **detargeting subsumes D7's "checkout at a generic path" generalisation** and at none of its cost, since a path arriving at run time is not in the closure either, while relocating one strands every existing volume's checkout. Honest limit, so nobody reads it as more: the guest *image* still knows the project's name until the seed is assignment-driven (§6.2), so finishing this does not make two targets concurrent. Three things already in place make it a refactor — the record writes `profile` and `class` and nothing reads them, and reserves `profile_snapshot` for the pin, `CAPSULE_REPO` is the run-time-override precedent already shipped, and the case suites already forced the guest-side scripts to take their subject as an argument. A second read for readiness added the rest: `stateNeedsUnit` is the same build-time coupling as `programVerbs` and the harder one — eleven of its twelve sites are the front end's *printed text*, and `capsule all status` is one table for the fleet — `host/observe.nix` and `host/baseline.nix` interpolate three values each and have **no case suite at all**, so step 1 as written pins three of five, and `guestPath` gains a second producer the moment it is read at run time. **Steps 1-2 done**: the five guest-pushed scripts (`state-snapshot`, `refresh`, `brief`'s runner, `observe`, `baseline`'s runner) take checkout, ceiling, command and declared paths on the command line, so `snapshotFor`/`refreshFor`/`runnerFor` are gone and each is one store path with three call sites instead of three renders; `guest-exec.nix`'s `loginRun` grew `"$@"` because a script on stdin had no argument channel at all, and a value crossing ssh is escaped **twice** since two shells parse it — `capsule-baseline`'s nested `bash -l -c "…"` was three and is the `"$0" "$@"` shape now. `observeCases` and `baselineCases` are new, `just cases` runs seven, and every suite was watched going red against a mutated copy. **A step 0 came out of doing them, and is built**: `flake.nix` was 2565 lines with 1341 (52%) case suites, ~530 of them added by steps 1-2 and more coming from 3-6 and D7 — so one file per suite beside what it pins (`host/<name>-cases.nix`), before step 3 and not during it, since otherwise 1350 lines move in the same commits that change what programs read. `flake.nix` 2565 → **1263**, seven files of 1384 lines under `host/`, attribute names and `just cases` unchanged, every case's log line byte-identical; the two suites handed a fixture rather than a shipped path (the guard's stubbed kernel, the front end's foreign pool) came off **unchanged store paths**. The probe fabric (~350 lines) stays, because `borrowed` throws where probes are built. **Step 3 done, and decisions 1 and 2 with it**: `host/profile.nix` renders `target.nix`'s run-time half to `<name>.json` per target (decision 1) into the **store** (decision 2), read back through one function — `profileLoad`, looking where `CAPSULE_PROFILE_DIR` says or at the baked render, which is `CAPSULE_REPO`'s shape generalised from an override to a lookup. Eleven checks at render and seven at read where there were none; `profileCases` is the eighth suite and the first whose subject is a *library*, so it splices the shipped fragment into the smallest `main` there is, and the first whose fixtures are read at **eval**, since a throw is not a build. The switch to a plain file turned out cheaper than the decision assumed — every one of the eleven checks is *intra-document*, so none of them needs anything nix knows. **Step 4 done, and decision 4 with it — the one the item did not have.** Which name a program loads: **an argument, `--profile <name>`, refusing without one**, and no default anywhere in a program, because on a host with two documents a fallback runs a verb against the wrong project's `guestPath` and `statePaths` and reports success having taken nothing ([47](./047-a-script-on-stdin-and-the-command-that-eats-it.md)'s shape, wider). The *name* is the **front end's** to resolve — explicit flag, then the slot's record `profile` field (inert since [29](./029-the-record-is-front-end-written.md)), then the one profile this host has rendered, refusing when none or several — which is `capsule <verb>`'s own unnamed-slot latitude one axis over and is a front end's alone ([20](./020-which-capsule-a-program-means.md)). `profile_snapshot` stays owed: bytes in a record are a second place `profileLoad` looks, which is what decision 2 bought. Mechanically it is step 2's seam changing type — each script's exported command-line tail from nix string to shell fragment, no *script* changed at all — and the escaping went **down by one**, since an array element at run time is parsed by the guest's shell and by no other. `gitChannelCases` is the ninth suite and the first over a program that talks to a guest; `snapshotCases` and `observeCases` gained the case that runs the shipped fragment's tail against the shipped script, which neither end could pin alone. `just cases` 322. The owed smoke test is **spent**: `capsule all status` on the live fleet and one collect off slot `e` — 30 files, 566,960 bytes. **Step 5 moved to the plain-file switch** rather than being deferred: [39](./039-a-bind-is-not-a-traversal.md)'s class cannot arise while the documents are a store path every uid can traverse and every reader is a program a human runs. **Step 6 done, and decision 3 with it — the one open since step 3.** *The front end's printed shape is not a function of any target*: every verb is offered, every column is printed, and what a target does not declare is a `-` in a cell or a run-time refusal naming the profile — because a header that changes when a document is edited is this item's own coupling one layer out, and neither of the other two answers (a table per target, a union of columns) has anything to say about the row an operator most wants, the **unassigned** slot. The rule has a boundary that makes it a rule: a *program* holds one profile and does branch on it, which is where `capsule-collect`'s two `--unit` refusals and `capsule-brief`'s usage line now live. Mechanically the step's two halves turned out to be **one**: once `needsUnit` is a question about a document, `host/state-snapshot.nix` takes no target value at all, so the hook is never null and `programVerbs`/`profileVerbs` fall out as literals — "the verb list could land alone" was false. `profileNeedsUnit` (host/profile.nix) is the only host-side spelling; one eval-time reader survives for `probe/two-capsules.sh` and no *program*. `slotNeedsUnit` has **three** answers — yes, no, and no target resolved — because a slot nothing has assigned has nothing for a token to be wrong against. **Three bugs found, none of them step 6's and none ever installed** (verified: the live `capsule-collect` rejects `--profile`, so it predates step 4): `recordProvisioned` grew a profile parameter at step 4 and `provision)` never grew the argument, so the ref landed in the profile's place and every provision died *after the push*; `setup)` passed a variable only the other `case` branch sets; and older than both, `oid=$(observed …)` under `pipefail` + `set -e` made the "guest did not answer for its HEAD" branch unreachable, so a provision against a quiet guest exited 1 in silence. All three are past a `work` call, so nothing but a stub could find them and no suite had ever run that path. Nine mutations, each red on its own rounds; one was rejected by shellcheck before the suite ran, which reads as "nothing went red". `just cases` **369**. Smoke test spent again: `capsule all status` on the live fleet and one collect off slot `e` — the same 30 files, 566,960 bytes. What it does **not** close: two targets are still a rebuild apart (decision 2), and the guest image still knows the project's name (§6.2) |
| [52](./052-the-document-leaves-the-store.md) | the document leaves the store, and a pin becomes necessary | **steps 1 and 2 built and green, step 3 owed; all three decisions taken** — [item 51](./051-the-target-in-four-store-paths.md) made every host-side program read the target's run-time half out of a document and put that document in `/nix/store`, which was its decision 2 and explicitly "a store path first". What that bought is one reader; what it left unbought is the property the whole arc was for — **two targets are still a rebuild apart**, and [Plan D](../plan-d-fleet.md) §6.1's controller that never runs `nixos-rebuild` is still impossible. This item moves the documents out and carries the two things item 51 deliberately deferred to the same commit: **step 5's `profileDir` pairing** and **`profile_snapshot`**, neither of which is real before it — with the documents in the store there is nothing that can edit one, so a pin has nothing to defend against. Three decisions, two already taken elsewhere. **Where they live**: a host-owned directory, with nix owning the names it renders and overwriting them at activation, because copy-if-absent makes a `target.nix` edit invisible forever ([item 22](./022-secrets-at-start.md)'s rule applied to a *derived* payload) and a store symlink gives up the point. Not a committed plain file read back with `builtins.fromJSON`, which would be one spelling rather than two: the guest image needs five of the fields, so a document nix reads is a document whose syntax errors break a rebuild, and a controller's bad write would take out a project it has nothing to do with. **Who validates**: one implementation, in the reader, with the build *running* the shipped fragment over what it rendered — `host/profile-cases.nix`'s own arrangement turned from a test into a build step, since once a document can arrive from elsewhere the eleven render checks protect only the documents that never needed it. **What `profile_snapshot` is**: the bytes, per [contract-assignment.md](../contract-assignment.md), because every name in a record re-resolves and a digest detects the drift it cannot undo — mechanically a file in the slot's own directory, so pinning is a `profileDir` choice and `profileLoad` never learns a second way to find bytes. Steps 1 and 2 are done: `host/services.nix` grows `profileDir`, a tmpfiles rule, an activation that installs one document per declared target, and `CAPSULE_PROFILE_DIR` in `wrap` — which is **item 51's owed step 5** — with one declaration of the directory and two consumers, since two carefully-equal spellings are the thing that drifts; and `host/profile.nix` splits into a **validator** (what a document is, takes a file) and a **locator** (where documents live), a seam reached for to break a cycle and better on its own terms, because the whole switch touches only the second. Nine rules became one jq program producing the first failing reason, `try`/`catch` so a wrong type is a refusal rather than a stack trace; two stayed in shell because they are about the loaded values. A **twelfth** rule appeared that only this commit could need: a document is addressed by its filename and also names itself, and while nix rendered every one nothing could make those disagree. `just cases` **384**. Three mutations, each red on its own rounds — and the one that neutered the grammar reddened 17, whose *shape* is the finding: the newline rule went red on its **reason only**, since the document is still refused downstream by the `mapfile` count, so a suite asserting exit status alone would have called it green. Two things are built and deliberately not asserted, said rather than covered by a round that would not discriminate: that the render invokes the checker, and that `wrap` exports the *right* directory — the second turned up item 37's class a third time, since `installed` forces the wrappers and builds nothing, so five programs whose entire text is an environment had never been shellchecked; `hostModulePrograms` now embeds them and the module's activation scripts, which also makes the rendered document a build input. Step 3 is `profile_snapshot`. What it closes is item 51's stated limit; what it does not is the guest **image**, which still knows the project's name (§6.2) |
| [53](./053-three-coarse-verbs.md) | three coarse verbs, and the words that must not enter them | **all four questions decided; the first prerequisite built and green, and so is the gap below** — `capsule <slot> fetch` answers for each half separately and prints the archive that unblocks a refusal ([50](./050-a-quarantine-outlives-its-assignment.md)'s third finding, and the half of decision 3 a human can run by hand); the automatic rename and the three verbs are unbuilt. Written the day the hand-run sequence failed in production: `SL-251` was landed from a collect four hours stale, three commits short of the guest that filled it, so the review body already committed in slot `d` never crossed, the reconcile re-derived it host-side under a second id, and the unit closed carrying two divergent reviews of itself. Nothing refused, because **nothing compares an exhibit against the guest it came from** — `status` prints the head, `branches` prints the tip, no verb reads both. Neighbour to [item 50](./050-a-quarantine-outlives-its-assignment.md) rather than an instance of it: 50 is a quarantine outliving its assignment, this is one lagging its source. The three acts doctrine treats as atomic — push a unit for implementation, begin a second capsule on it, accept the result — are several verbs and a habit today, and the habit is the part that failed. The vocabulary question costs nothing: doctrine's **slice** is the record's `unit` ([32](./032-the-sideband-channel.md)) and its **audit** is `purpose`, so no program learns a target's workflow words. Shape: `setup <ref> --unit --purpose --state-from-host`, a new `handoff <src>`, and `land`; only the middle is machinery that does not exist. Five invariants the composites must own, each supplied by hand on the day — verify the exhibit against its guest, **archive before forcing** (the order is what makes the force safe), clear the destination's stale stage ref, carry `unit` and require `purpose`, and one provision carrying its state ([47](./047-a-script-on-stdin-and-the-command-that-eats-it.md)). `land` stops at refs, because which branch a result belongs on is the target's governance. All three belong in the **front end** ([20](./020-which-capsule-a-program-means.md)): a `handoff` program would read host state three times inside a thing that must not do it once. **One gap found while reading, now built**: `provision)`, `collect)` and `brief)` filled `--unit` from the record and `setup)` did not, so `setup <ref> --state-from-host` was refused where the same flags on `provision` succeeded — fixed as one construction and not a fourth copy, and it turned out to be two: **`unitScope`** is the interception at all four call sites (it *prints* the two words rather than mutating argv, so the record still reads the ref that was asked for; `carrier` is the argv word meaning this invocation carries host-side state, `-` where it is unconditional), and **`provisionSlot`** is the four steps a provision *is*, since `setup` is a provision with two steps after it and had its own copy of those four minus the scope. Nine rounds in `policyCases`; four mutations each red on its own rounds, and a fifth **rejected by shellcheck before the suite ran**. Still verb 1's and unbuilt: `setup --unit`/`--purpose` as record writes. **Four decisions.** `land` writes **no branch** unless handed a name, which then refuses an existing one and has no default, and its report is divergence against the target repo's **current `HEAD`** — a fact about that repo rather than a value of ours, so it can be printed without choosing anything. `handoff` refuses on a **modified tracked file and only that**, because the two dirt classes have different fates in `host/state-snapshot.nix` — untracked-not-ignored travels as content, a modified tracked file travels only as `.capsule/dirty.diff`, *a record of a worktree rather than part of one* — so the second stands a capsule up on code its source moved past, which is this item's own trap in miniature; and the check is read **from the exhibit**, so it needs no second round trip and survives the source going down. The archive before the force is **generation-keyed in the human's repo** (`refs/capsule/<dst>/gen/<n>/…`, renamed by the front end off the record it supersedes), which makes [item 50](./050-a-quarantine-outlives-its-assignment.md)'s minimal half a **prerequisite** rather than a neighbour: a composite that runs 50's sequence for you produces its measured outcome — assignment 1's code beside assignment 2's state — silently and every time. The quarantine's superseded copy is let go on purpose, since a quarantine is what a capsule sent back and not where state lives ([42](./042-a-state-half-no-capsule-has-held.md)). And the verify is a **refusal with no `--stale`**, whose corollary is stated rather than discovered: `handoff` and `land` **need the source running**, because the guest's head is the only place the comparison exists — a slot stopped since its last collect is unverifiable, and whether `stop` should record that head is the record's question |
