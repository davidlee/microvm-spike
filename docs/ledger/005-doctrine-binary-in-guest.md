# NOTES item 5 — no `doctrine` binary in the guest

*State: resolved — `dev-tools`.*
One item of the [ledger](./index.md) — the number is the citation, and it
never moves.

~~No `doctrine` binary in the guest~~ — resolved: `dev-tools` carries it,
along with jujutsu, sccache, graphviz/d2/mermaid and the rest of the
devshell's list. Watch the store disk size.
